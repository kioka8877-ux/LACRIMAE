# SHARED/IN/logos — Logo de campagne (mode forge)

Dépose ici le logo de la campagne (image **PNG fond transparent**), fourni par
le propriétaire de la caméra. Il est calé sur le calque logo (au-dessus du clip,
sous les presets) dans la preview F03 et le render F04.

- Fichier attendu : `logo.png` (un seul)
- Transité automatiquement en mode forge (G4 du workflow)
