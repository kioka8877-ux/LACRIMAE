modal>=1.0,<2
numpy==1.26.4
opencv-python-headless==4.10.0.84
boto3==1.35.99

# PyTorch + CUDA
torch==2.5.1
torchvision==0.20.1

# Phase 0 — RIFE (interpolation temporelle)
# (installé depuis le Volume modèles, pas via pip)

# Phase 1 — Restauration IA
basicsr==1.4.2
realesrgan==0.3.0
# SCUNet — débruitage blind + dehalo
# Installer depuis GitHub : pip install git+https://github.com/cszn/SCUNet.git
scunet @ git+https://github.com/cszn/SCUNet.git

# Phase 2 — Restauration faciale
gfpgan==1.3.8

# Phase 3 — Color grading ACES
# colour-science pour les transforms ACES → Rec.709
colour-science>=0.4.4

# Phase 5 — Bloom (OpenCV, déjà inclus via opencv-python-headless)
